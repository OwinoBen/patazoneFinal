define(['./knockout', './template/engine', './knockout-es5', './bootstrap', './observable_array', './bound-nodes', 'domReady!'], function (ko, templateEngine) {
    'use strict';
    ko.uid = 0;
    ko.setTemplateEngine(templateEngine);
    ko.applyBindings();
});